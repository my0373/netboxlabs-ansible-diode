---
# Copy this file to credentials.yml and fill in your values.
# NEVER commit credentials.yml to version control.
#
# Alternatively, export these as environment variables:
#   export DIODE_TARGET="grpcs://your-instance.cloud.netboxapp.com/diode"
#   export DIODE_CLIENT_ID="your-client-id"
#   export DIODE_CLIENT_SECRET="your-client-secret"

diode_target: "grpcs://your-instance.cloud.netboxapp.com/diode"
diode_client_id: "your-client-id"
diode_client_secret: "your-client-secret"
